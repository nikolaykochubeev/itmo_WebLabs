const preloader = document.getElementById('preloader');const content = document.getElementById('comments');preloader.style.display = 'block';window.addEventListener('load', async () => {    try {        await fetch('https://jsonplaceholder.typicode.com/comments')            .then(response => response.json())            .then(data => {                const filteredData = Math.random() < 0.5                    ? data.filter(item => item.id >= 100)                    : data.filter(item => item.id < 200);                content.innerHTML = `                <ul>                    ${filteredData.slice(0, 1000).map(item => `<li><h2>${item.name}</h2><h2>${item.email}</h2> <p>${item.body}</p></li>`).join('')}                </ul>`;            })    } catch (e) {        alert(+ e.name + ":" + e.message + "\n" + e.stack);    }    preloader.style.display = 'none'})